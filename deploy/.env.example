# Production secrets for closet-backend. Copy to .env and fill in real values.
# DO NOT commit .env (gitignored). The docker compose file refuses to start
# without these set.

DB_PASSWORD=change-me-strong-password

MINIO_USER=closet
MINIO_PASSWORD=change-me-strong-secret
